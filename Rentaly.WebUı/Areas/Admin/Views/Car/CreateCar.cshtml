@using Rentaly.EntityLayer.Entities
@model Car
@{
    ViewData["Title"] = "CreateCar";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@section Styles {
    <style>
        .form-section {
            background: #1a1a1a;
            border: 1px solid var(--border);
            border-radius: var(--radius);
            box-shadow: var(--shadow-sm);
            margin-bottom: 20px;
            overflow: hidden;
            transition: var(--transition);
        }

            .form-section:hover {
                box-shadow: var(--shadow-md);
            }

        .section-header {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 18px 24px;
            border-bottom: 1px solid var(--border);
            background: #252525;
        }

        .section-icon {
            width: 38px;
            height: 38px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            flex-shrink: 0;
        }

        .section-title {
            font-family: 'Plus Jakarta Sans',sans-serif;
            font-size: 14.5px;
            font-weight: 700;
            color: var(--text-main);
            margin: 0;
        }

        .section-subtitle {
            font-size: 12px;
            color: var(--text-muted);
            margin: 0;
            margin-top: 1px;
        }

        .section-body {
            padding: 24px;
        }

        .form-label {
            font-size: 13px;
            font-weight: 600;
            color: var(--text-main);
            margin-bottom: 7px;
            display: flex;
            align-items: center;
            gap: 5px;
        }

            .form-label .required {
                color: var(--danger);
                font-size: 14px;
                line-height: 1;
            }

        .form-control, .form-select {
            font-family: 'DM Sans',sans-serif;
            font-size: 13.5px;
            color: #fff;
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            padding: 10px 14px;
            background: #2a2a2a;
            transition: var(--transition);
            outline: none;
            width: 100%;
        }

            .form-control:focus, .form-select:focus {
                border-color: var(--primary);
                box-shadow: 0 0 0 3px rgba(204,12,12,0.15);
            }

            .form-control::placeholder {
                color: #808080;
            }

            .form-control.is-invalid, .form-select.is-invalid {
                border-color: var(--danger);
            }

            .form-control.is-valid, .form-select.is-valid {
                border-color: var(--success);
            }

        .invalid-feedback {
            font-size: 12px;
            color: var(--danger);
            margin-top: 5px;
            display: none;
            align-items: center;
            gap: 4px;
        }

            .invalid-feedback.show {
                display: flex !important;
            }

        .form-hint {
            font-size: 12px;
            color: var(--text-muted);
            margin-top: 5px;
        }

        .input-icon-wrap {
            position: relative;
        }

            .input-icon-wrap .input-icon {
                position: absolute;
                left: 13px;
                top: 50%;
                transform: translateY(-50%);
                color: var(--text-muted);
                font-size: 14px;
                pointer-events: none;
            }

            .input-icon-wrap .form-control {
                padding-left: 38px;
            }

            .input-icon-wrap:focus-within .input-icon {
                color: var(--primary);
            }

        .input-suffix {
            position: absolute;
            right: 0;
            top: 0;
            bottom: 0;
            display: flex;
            align-items: center;
            padding: 0 14px;
            font-size: 13px;
            font-weight: 600;
            color: var(--text-muted);
            border-left: 1px solid var(--border);
            background: #2a2a2a;
            border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
            pointer-events: none;
        }

        .quick-add-row {
            display: flex;
            align-items: center;
            gap: 8px;
        }

            .quick-add-row .form-select {
                flex: 1;
            }

        .btn-quick-add {
            flex-shrink: 0;
            width: 38px;
            height: 42px;
            border: 1.5px solid var(--primary);
            background: var(--primary-light);
            color: var(--primary);
            border-radius: var(--radius-sm);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            cursor: pointer;
            transition: var(--transition);
        }

            .btn-quick-add:hover {
                background: var(--primary);
                color: #fff;
            }

        .quick-modal-backdrop {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0,0,0,.65);
            z-index: 9990;
            align-items: center;
            justify-content: center;
        }

            .quick-modal-backdrop.open {
                display: flex;
            }

        .quick-modal {
            background: #1a1a1a;
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 28px;
            width: 100%;
            max-width: 420px;
            box-shadow: 0 24px 60px rgba(0,0,0,.5);
            animation: modalIn .2s ease;
        }

        @@keyframes modalIn {
            from {
                opacity: 0;
                transform: translateY(-12px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .quick-modal-title {
            font-family: 'Plus Jakarta Sans',sans-serif;
            font-size: 16px;
            font-weight: 800;
            color: var(--text-main);
            margin-bottom: 18px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

            .quick-modal-title i {
                color: var(--primary);
            }

        .quick-modal-footer {
            display: flex;
            gap: 10px;
            justify-content: flex-end;
            margin-top: 20px;
        }

        .btn-modal-cancel {
            padding: 8px 18px;
            border: 1px solid var(--border);
            background: transparent;
            color: var(--text-muted);
            border-radius: var(--radius-sm);
            font-size: 13px;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
        }

            .btn-modal-cancel:hover {
                background: #2a2a2a;
                color: var(--text-main);
            }

        .btn-modal-save {
            padding: 8px 22px;
            background: var(--primary);
            color: #fff;
            border: none;
            border-radius: var(--radius-sm);
            font-size: 13px;
            font-weight: 700;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 6px;
        }

            .btn-modal-save:hover {
                background: var(--primary-dark);
            }

            .btn-modal-save:disabled {
                opacity: .6;
                cursor: not-allowed;
            }

        .quick-modal-error {
            font-size: 12px;
            color: var(--danger);
            margin-top: 10px;
            display: none;
        }

        .fuel-options {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
        }

        .fuel-radio {
            display: none;
        }

            .fuel-radio + label {
                display: flex;
                align-items: center;
                gap: 7px;
                padding: 8px 16px;
                border: 1.5px solid var(--border);
                border-radius: 40px;
                font-size: 13px;
                font-weight: 600;
                color: var(--text-muted);
                cursor: pointer;
                transition: var(--transition);
                user-select: none;
            }

                .fuel-radio + label:hover {
                    border-color: var(--primary);
                    color: var(--primary);
                    background: var(--primary-light);
                }

            .fuel-radio:checked + label {
                border-color: var(--primary);
                background: var(--primary);
                color: #fff;
            }

        .counter-input {
            display: flex;
            align-items: center;
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            overflow: hidden;
            width: fit-content;
        }

        .counter-btn {
            width: 38px;
            height: 42px;
            border: none;
            background: #2a2a2a;
            color: var(--text-muted);
            font-size: 16px;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
        }

            .counter-btn:hover {
                background: var(--primary-light);
                color: var(--primary);
            }

        .counter-val {
            width: 52px;
            height: 42px;
            border: none;
            border-left: 1px solid var(--border);
            border-right: 1px solid var(--border);
            text-align: center;
            font-family: 'Plus Jakarta Sans',sans-serif;
            font-size: 15px;
            font-weight: 700;
            color: var(--text-main);
            outline: none;
            background: #1a1a1a;
        }

        .toggle-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 14px 0;
            border-bottom: 1px solid var(--border);
        }

            .toggle-row:last-child {
                border-bottom: none;
                padding-bottom: 0;
            }

            .toggle-row:first-child {
                padding-top: 0;
            }

        .toggle-label strong {
            font-size: 13.5px;
            font-weight: 600;
            color: var(--text-main);
            display: block;
        }

        .toggle-label span {
            font-size: 12px;
            color: var(--text-muted);
        }

        .form-switch .form-check-input {
            width: 44px;
            height: 24px;
            cursor: pointer;
            background-color: #3a3a3a;
            border-color: var(--border);
        }

            .form-switch .form-check-input:checked {
                background-color: var(--primary);
                border-color: var(--primary);
            }

        .upload-zone {
            border: 2px dashed var(--border);
            border-radius: var(--radius);
            padding: 32px 20px;
            text-align: center;
            cursor: pointer;
            transition: var(--transition);
            background: #252525;
            position: relative;
        }

            .upload-zone:hover, .upload-zone.dragover {
                border-color: var(--primary);
                background: var(--primary-light);
            }

            .upload-zone input[type="file"] {
                position: absolute;
                inset: 0;
                opacity: 0;
                cursor: pointer;
                width: 100%;
                height: 100%;
            }

        .upload-icon {
            width: 52px;
            height: 52px;
            background: var(--primary-light);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 12px;
            font-size: 20px;
            color: var(--primary);
            transition: var(--transition);
        }

        .upload-zone:hover .upload-icon {
            background: var(--primary);
            color: #fff;
        }

        .upload-title {
            font-family: 'Plus Jakarta Sans',sans-serif;
            font-size: 14px;
            font-weight: 700;
            color: var(--text-main);
            margin-bottom: 4px;
        }

        .upload-sub {
            font-size: 12.5px;
            color: var(--text-muted);
        }

        .img-tabs {
            display: flex;
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            overflow: hidden;
            margin-bottom: 14px;
        }

        .img-tab-btn {
            flex: 1;
            padding: 8px;
            font-size: 13px;
            font-weight: 600;
            font-family: 'DM Sans',sans-serif;
            border: none;
            cursor: pointer;
            transition: var(--transition);
        }

            .img-tab-btn.active {
                background: var(--primary);
                color: #fff;
            }

            .img-tab-btn:not(.active) {
                background: #2a2a2a;
                color: var(--text-muted);
            }

        .price-banner {
            background: linear-gradient(135deg,#7f0000,#cc0c0c);
            border-radius: var(--radius);
            padding: 16px 20px;
            color: #fff;
            display: none;
        }

        .sum-row {
            display: none;
            font-size: 13px;
            padding: 8px 0;
            border-bottom: 1px solid var(--border);
        }

            .sum-row:last-of-type {
                border-bottom: none;
            }

        .action-bar {
            position: sticky;
            bottom: 0;
            background: #1a1a1a;
            border-top: 1px solid var(--border);
            padding: 14px 0;
            margin-top: 4px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 10px;
            z-index: 100;
            box-shadow: 0 -4px 20px rgba(0,0,0,.2);
        }

        .btn-cancel-form {
            display: inline-flex;
            align-items: center;
            gap: 7px;
            background: #1a1a1a;
            border: 1px solid var(--border);
            color: var(--text-muted);
            border-radius: var(--radius-sm);
            padding: 10px 20px;
            font-size: 13.5px;
            font-weight: 600;
            font-family: 'DM Sans',sans-serif;
            cursor: pointer;
            transition: var(--transition);
            text-decoration: none;
        }

            .btn-cancel-form:hover {
                background: #2a2a2a;
                color: var(--text-main);
            }

        .btn-submit-car {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: var(--primary);
            color: #fff;
            border: none;
            border-radius: var(--radius-sm);
            padding: 10px 24px;
            font-size: 13.5px;
            font-weight: 700;
            font-family: 'DM Sans',sans-serif;
            cursor: pointer;
            transition: var(--transition);
        }

            .btn-submit-car:hover {
                background: var(--primary-dark);
                box-shadow: 0 4px 14px rgba(204,12,12,0.4);
                transform: translateY(-1px);
            }

            .btn-submit-car:disabled {
                opacity: .7;
                cursor: not-allowed;
                transform: none;
            }

        .plate-preview {
            display: inline-flex;
            align-items: center;
            border: 2px solid #333;
            border-radius: 6px;
            overflow: hidden;
            margin-top: 10px;
        }

        .plate-flag {
            background: #003399;
            color: #FFD700;
            font-size: 9px;
            font-weight: 800;
            padding: 4px 6px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .plate-number {
            background: #f5f5f5;
            color: #111;
            font-size: 18px;
            font-weight: 900;
            padding: 4px 12px;
            letter-spacing: 3px;
            min-width: 110px;
            text-align: center;
        }

        .char-count {
            font-size: 11.5px;
            color: var(--text-muted);
            text-align: right;
            margin-top: 4px;
        }

        textarea.form-control {
            resize: vertical;
            min-height: 90px;
        }

        @@media (max-width:767px) {
            .section-body {
                padding: 14px;
            }

            .action-bar {
                padding: 12px 0;
            }
        }
    </style>
}

<div class="page-header">
    <div>
        <h1 class="page-title">Yeni Araç Ekle</h1>
        <p class="page-subtitle">Filoya yeni bir araç kaydı oluşturun.</p>
    </div>
    <a href="/Admin/Car/CarList" class="btn-cancel-form">
        <i class="fa-solid fa-arrow-left"></i> Listeye Dön
    </a>
</div>

<form id="carForm" asp-action="CreateCar" asp-controller="Car" asp-area="Admin"
      method="post" enctype="multipart/form-data" novalidate>
    @Html.AntiForgeryToken()

    <div class="row g-4">
        <div class="col-12 col-xl-8">

            @* 1 · Kimlik *@
            <div class="form-section">
                <div class="section-header">
                    <div class="section-icon" style="background:#2d0a0a;color:var(--primary);"><i class="fa-solid fa-id-card"></i></div>
                    <div><p class="section-title">Araç Kimlik Bilgileri</p><p class="section-subtitle">Plaka ve şasi numarası</p></div>
                </div>
                <div class="section-body">
                    <div class="row g-3">
                        <div class="col-12 col-md-6">
                            <label class="form-label" for="PlateNumber">Plaka <span class="required">*</span></label>
                            <div class="input-icon-wrap">
                                <input type="text" id="PlateNumber" name="PlateNumber" class="form-control"
                                       placeholder="34 ABC 123" maxlength="15" autocomplete="off"
                                       style="font-weight:700;letter-spacing:2px;font-family:'Plus Jakarta Sans',sans-serif;"
                                       oninput="this.value=this.value.toUpperCase();updatePlatePreview();validatePlateInput(this)" />
                                <i class="fa-solid fa-hashtag input-icon"></i>
                            </div>
                            <div class="invalid-feedback" id="plateError"><i class="fa-solid fa-circle-exclamation"></i> Plaka zorunludur.</div>
                            <div id="platePreviewWrap" style="display:none;margin-top:10px;">
                                <div class="plate-preview">
                                    <div class="plate-flag"><span>🇹🇷</span><small>TR</small></div>
                                    <div class="plate-number" id="platePreviewText">— — —</div>
                                </div>
                            </div>
                        </div>
                        <div class="col-12 col-md-6">
                            <label class="form-label" for="VIN">Şasi No (VIN) <span class="required">*</span></label>
                            <div class="input-icon-wrap">
                                <input type="text" id="VIN" name="VIN" class="form-control"
                                       placeholder="WBA5A5C5XED123456" maxlength="50" autocomplete="off"
                                       style="font-family:monospace;letter-spacing:1px;font-size:13px;"
                                       oninput="this.value=this.value.toUpperCase();updateVinCount();liveValidate(this)" />
                                <i class="fa-solid fa-barcode input-icon"></i>
                            </div>
                            <div class="d-flex justify-content-between mt-1">
                                <div class="invalid-feedback" id="vinError"><i class="fa-solid fa-circle-exclamation"></i> VIN zorunludur.</div>
                                <div class="char-count ms-auto"><span id="vinCount">0</span>/50</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            @* 2 · Sınıflandırma *@
            <div class="form-section">
                <div class="section-header">
                    <div class="section-icon" style="background:#2d2d0a;color:var(--accent);"><i class="fa-solid fa-layer-group"></i></div>
                    <div><p class="section-title">Sınıflandırma</p><p class="section-subtitle">Bulunamayanı <strong style="color:var(--primary);">+</strong> ile hemen ekleyin</p></div>
                </div>
                <div class="section-body">
                    <div class="row g-3">
                        <div class="col-12 col-sm-6">
                            <label class="form-label" for="BrandId">Marka <span class="required">*</span></label>
                            <div class="quick-add-row">
                                <select id="BrandId" name="BrandId" class="form-select" onchange="loadModels(this.value);liveValidate(this);updateSummary()">
                                    <option value="">— Marka Seçin —</option>
                                    @foreach (var b in ViewBag.Brands as List<Brand> ?? new List<Brand>())
                                    {
                                        <option value="@b.BrandId">@b.BrandName</option>
                                    }
                                </select>
                                <button type="button" class="btn-quick-add" onclick="openModal('brand')" title="Yeni marka ekle"><i class="fa-solid fa-plus"></i></button>
                            </div>
                            <div class="invalid-feedback" id="brandError"><i class="fa-solid fa-circle-exclamation"></i> Marka seçimi zorunludur.</div>
                        </div>
                        <div class="col-12 col-sm-6">
                            <label class="form-label" for="CarModelId">Model <span class="required">*</span></label>
                            <div class="quick-add-row">
                                <select id="CarModelId" name="CarModelId" class="form-select" onchange="liveValidate(this);updateSummary()">
                                    <option value="">— Önce marka seçin —</option>
                                </select>
                                <button type="button" class="btn-quick-add" onclick="openModal('model')" title="Yeni model ekle"><i class="fa-solid fa-plus"></i></button>
                            </div>
                            <div class="invalid-feedback" id="modelError"><i class="fa-solid fa-circle-exclamation"></i> Model seçimi zorunludur.</div>
                        </div>
                        <div class="col-12 col-sm-6">
                            <label class="form-label" for="CategoryId">Kategori <span class="required">*</span></label>
                            <div class="quick-add-row">
                                <select id="CategoryId" name="CategoryId" class="form-select" onchange="liveValidate(this)">
                                    <option value="">— Kategori Seçin —</option>
                                    @foreach (var c in ViewBag.Categories as List<Category> ?? new List<Category>())
                                    {
                                        <option value="@c.CategoryId">@c.CategoryName</option>
                                    }
                                </select>
                                <button type="button" class="btn-quick-add" onclick="openModal('category')" title="Yeni kategori ekle"><i class="fa-solid fa-plus"></i></button>
                            </div>
                            <div class="invalid-feedback" id="categoryError"><i class="fa-solid fa-circle-exclamation"></i> Kategori seçimi zorunludur.</div>
                        </div>
                        <div class="col-12 col-sm-6">
                            <label class="form-label" for="BranchId">Şube <span class="required">*</span></label>
                            <div class="quick-add-row">
                                <select id="BranchId" name="BranchId" class="form-select" onchange="liveValidate(this)">
                                    <option value="">— Şube Seçin —</option>
                                    @foreach (var br in ViewBag.Branches as List<Branch> ?? new List<Branch>())
                                    {
                                        <option value="@br.BranchId">@br.BranchName - @br.City</option>
                                    }
                                </select>
                                <button type="button" class="btn-quick-add" onclick="openModal('branch')" title="Yeni şube ekle"><i class="fa-solid fa-plus"></i></button>
                            </div>
                            <div class="invalid-feedback" id="branchError"><i class="fa-solid fa-circle-exclamation"></i> Şube seçimi zorunludur.</div>
                        </div>
                    </div>
                </div>
            </div>

            @* 3 · Teknik *@
            <div class="form-section">
                <div class="section-header">
                    <div class="section-icon" style="background:#0a2d0a;color:#10b981;"><i class="fa-solid fa-screwdriver-wrench"></i></div>
                    <div><p class="section-title">Teknik Detaylar</p><p class="section-subtitle">Yıl, kilometre, yakıt ve koltuk</p></div>
                </div>
                <div class="section-body">
                    <div class="row g-3">
                        <div class="col-12 col-sm-6 col-md-4">
                            <label class="form-label" for="Year">Model Yılı <span class="required">*</span></label>
                            <div class="input-icon-wrap">
                                <input type="number" id="Year" name="Year" class="form-control" placeholder="2024" min="1990" max="2030" oninput="liveValidate(this);updateSummary()" />
                                <i class="fa-solid fa-calendar input-icon"></i>
                            </div>
                            <div class="invalid-feedback" id="yearError"><i class="fa-solid fa-circle-exclamation"></i> Yıl zorunludur.</div>
                        </div>
                        <div class="col-12 col-sm-6 col-md-4">
                            <label class="form-label" for="Kilometer">Kilometre <span class="required">*</span></label>
                            <div class="input-icon-wrap" style="position:relative;">
                                <input type="number" id="Kilometer" name="Kilometer" class="form-control" placeholder="0" min="0" style="padding-right:50px;" oninput="liveValidate(this)" />
                                <i class="fa-solid fa-road input-icon"></i>
                                <span class="input-suffix">km</span>
                            </div>
                            <div class="invalid-feedback" id="kmError"><i class="fa-solid fa-circle-exclamation"></i> Kilometre zorunludur.</div>
                        </div>
                        <div class="col-12 col-sm-6 col-md-4">
                            <label class="form-label" for="Transmission">Vites <span class="required">*</span></label>
                            <select id="Transmission" name="Transmission" class="form-select" onchange="liveValidate(this);updateSummary()">
                                <option value="">— Seçin —</option>
                                <option value="Otomatik">Otomatik</option>
                                <option value="Manuel">Manuel</option>
                                <option value="Yarı Otomatik">Yarı Otomatik</option>
                                <option value="CVT">CVT</option>
                            </select>
                            <div class="invalid-feedback" id="transError"><i class="fa-solid fa-circle-exclamation"></i> Vites tipi zorunludur.</div>
                        </div>
                        <div class="col-12">
                            <label class="form-label">Yakıt Tipi <span class="required">*</span></label>
                            <div class="fuel-options">
                                @foreach (var f in new[] { ("Benzin", "fa-gas-pump", "fuel_benzin"), ("Dizel", "fa-fill-drip", "fuel_dizel"), ("Elektrik", "fa-bolt", "fuel_elektrik"), ("Hibrit", "fa-leaf", "fuel_hibrit"), ("LPG", "fa-fire-flame-curved", "fuel_lpg") })
                                {
                                    <input type="radio" class="fuel-radio" name="FuelType" id="@f.Item3" value="@f.Item1" />
                                    <label for="@f.Item3"><i class="fa-solid @f.Item2"></i> @f.Item1</label>
                                }
                            </div>
                            <div class="invalid-feedback" id="fuelError"><i class="fa-solid fa-circle-exclamation"></i> Yakıt tipi zorunludur.</div>
                        </div>
                        <div class="col-12 col-sm-4">
                            <label class="form-label">Koltuk Sayısı <span class="required">*</span></label>
                            <div class="counter-input">
                                <button type="button" class="counter-btn" onclick="adjustCounter('SeatCount',-1)"><i class="fa-solid fa-minus"></i></button>
                                <input type="number" id="SeatCount" name="SeatCount" class="counter-val" value="5" min="1" max="20" readonly />
                                <button type="button" class="counter-btn" onclick="adjustCounter('SeatCount',1)"><i class="fa-solid fa-plus"></i></button>
                            </div>
                            <p class="form-hint"><i class="fa-solid fa-circle-info me-1" style="color:var(--primary);"></i>Sürücü dahil</p>
                        </div>
                        <div class="col-12 col-sm-4">
                            <label class="form-label">Yolcu Kapasitesi <span class="required">*</span></label>
                            <div class="counter-input">
                                <button type="button" class="counter-btn" onclick="adjustCounter('PersonCount',-1)"><i class="fa-solid fa-minus"></i></button>
                                <input type="number" id="PersonCount" name="PersonCount" class="counter-val" value="4" min="1" max="20" readonly />
                                <button type="button" class="counter-btn" onclick="adjustCounter('PersonCount',1)"><i class="fa-solid fa-plus"></i></button>
                            </div>
                            <p class="form-hint"><i class="fa-solid fa-circle-info me-1" style="color:var(--primary);"></i>Sürücü hariç</p>
                        </div>
                        <div class="col-12 col-sm-4">
                            <label class="form-label">Bagaj Kapasitesi <span class="required">*</span></label>
                            <div class="counter-input">
                                <button type="button" class="counter-btn" onclick="adjustCounter('LuggageCount',-1)"><i class="fa-solid fa-minus"></i></button>
                                <input type="number" id="LuggageCount" name="LuggageCount" class="counter-val" value="2" min="0" max="10" readonly />
                                <button type="button" class="counter-btn" onclick="adjustCounter('LuggageCount',1)"><i class="fa-solid fa-plus"></i></button>
                            </div>
                            <p class="form-hint"><i class="fa-solid fa-circle-info me-1" style="color:var(--primary);"></i>Orta boy valiz</p>
                        </div>
                    </div>
                </div>
            </div>

            @* 4 · Fiyatlandırma *@
            <div class="form-section">
                <div class="section-header">
                    <div class="section-icon" style="background:#2d2d0a;color:#d97706;"><i class="fa-solid fa-turkish-lira-sign"></i></div>
                    <div><p class="section-title">Fiyatlandırma</p><p class="section-subtitle">Günlük ücret ve depozito</p></div>
                </div>
                <div class="section-body">
                    <div class="row g-3">
                        <div class="col-12 col-sm-6">
                            <label class="form-label" for="DailyPrice">Günlük Ücret <span class="required">*</span></label>
                            <div class="input-icon-wrap" style="position:relative;">
                                <input type="number" id="DailyPrice" name="DailyPrice" class="form-control" placeholder="0.00" min="0" step="0.01" style="padding-right:42px;" oninput="updatePriceBanner();liveValidate(this);updateSummary()" />
                                <i class="fa-solid fa-turkish-lira-sign input-icon"></i>
                                <span class="input-suffix">₺</span>
                            </div>
                            <div class="invalid-feedback" id="priceError"><i class="fa-solid fa-circle-exclamation"></i> Günlük fiyat zorunludur.</div>
                        </div>
                        <div class="col-12 col-sm-6">
                            <label class="form-label" for="DepositAmount">Depozito <span class="required">*</span></label>
                            <div class="input-icon-wrap" style="position:relative;">
                                <input type="number" id="DepositAmount" name="DepositAmount" class="form-control" placeholder="0.00" min="0" step="0.01" style="padding-right:42px;" oninput="updatePriceBanner();liveValidate(this)" />
                                <i class="fa-solid fa-shield-halved input-icon"></i>
                                <span class="input-suffix">₺</span>
                            </div>
                            <div class="invalid-feedback" id="depositError"><i class="fa-solid fa-circle-exclamation"></i> Depozito zorunludur.</div>
                        </div>
                        <div class="col-12">
                            <div class="price-banner" id="priceBanner">
                                <div class="row g-2 text-center">
                                    <div class="col-4"><div style="font-size:10px;opacity:.75;font-weight:600;text-transform:uppercase;margin-bottom:4px;">Günlük</div><div style="font-family:'Plus Jakarta Sans',sans-serif;font-size:20px;font-weight:800;" id="bannerDaily">—</div></div>
                                    <div class="col-4"><div style="font-size:10px;opacity:.75;font-weight:600;text-transform:uppercase;margin-bottom:4px;">Haftalık</div><div style="font-family:'Plus Jakarta Sans',sans-serif;font-size:20px;font-weight:800;" id="bannerWeekly">—</div></div>
                                    <div class="col-4"><div style="font-size:10px;opacity:.75;font-weight:600;text-transform:uppercase;margin-bottom:4px;">Aylık</div><div style="font-family:'Plus Jakarta Sans',sans-serif;font-size:20px;font-weight:800;" id="bannerMonthly">—</div></div>
                                </div>
                                <div style="margin-top:10px;padding-top:10px;border-top:1px solid rgba(255,255,255,.2);font-size:12.5px;opacity:.85;display:flex;align-items:center;gap:6px;">
                                    <i class="fa-solid fa-shield-halved"></i> Depozito: <strong id="bannerDeposit">—</strong>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            @* 5 · Açıklama *@
            <div class="form-section">
                <div class="section-header">
                    <div class="section-icon" style="background:#0a1a2d;color:#3b82f6;"><i class="fa-solid fa-align-left"></i></div>
                    <div><p class="section-title">Açıklama</p><p class="section-subtitle">İsteğe bağlı</p></div>
                </div>
                <div class="section-body">
                    <textarea id="Description" name="Description" class="form-control" placeholder="Araç özellikleri, ekipmanlar, özel notlar…" maxlength="1000" oninput="document.getElementById('descCount').textContent=this.value.length"></textarea>
                    <div class="char-count"><span id="descCount">0</span>/1000</div>
                </div>
            </div>

        </div>

        <div class="col-12 col-xl-4">

            @* Görsel *@
            <div class="form-section">
                <div class="section-header">
                    <div class="section-icon" style="background:#2d0a2d;color:#9333ea;"><i class="fa-solid fa-image"></i></div>
                    <div><p class="section-title">Araç Görselleri</p><p class="section-subtitle">Birden fazla görsel yükleyebilirsiniz</p></div>
                </div>
                <div class="section-body">
                    <div class="img-tabs">
                        <button type="button" class="img-tab-btn active" id="tabUpload" onclick="switchImgTab('upload')"><i class="fa-solid fa-upload me-1"></i>Dosya</button>
                        <button type="button" class="img-tab-btn" id="tabUrl" onclick="switchImgTab('url')"><i class="fa-solid fa-link me-1"></i>URL</button>
                    </div>
                    <div id="uploadPanel">
                        <div class="upload-zone" id="uploadZone" ondragover="handleDragOver(event)" ondrop="handleDrop(event)" ondragleave="handleDragLeave(event)">
                            <input type="file" id="imageFiles" name="imageFiles" accept="image/*" multiple onchange="handleFileSelect(event)" />
                            <div class="upload-icon"><i class="fa-solid fa-cloud-arrow-up"></i></div>
                            <div class="upload-title">Görseli sürükleyin veya seçin</div>
                            <div class="upload-sub">JPG, PNG, WEBP — çoklu — maks. 5 MB</div>
                        </div>
                        <div id="imgPreviewWrap" style="display:none;">
                            <div id="imgPreviewGrid" style="display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-top:12px;"></div>
                            <button type="button" onclick="clearImages()" style="margin-top:8px;border:none;background:none;color:var(--danger);font-size:12px;font-weight:600;cursor:pointer;"><i class="fa-solid fa-xmark me-1"></i>Tümünü Kaldır</button>
                        </div>
                    </div>
                    <div id="urlPanel" style="display:none;">
                        <label class="form-label" for="CoverImageUrl">Kapak Görseli URL</label>
                        <input type="url" id="CoverImageUrl" name="CoverImageUrl" class="form-control" placeholder="https://…" oninput="previewUrlImg(this.value)" />
                        <div id="urlPreviewWrap" style="display:none;margin-top:10px;">
                            <img id="urlImgPreview" src="" alt="" style="width:100%;max-height:180px;object-fit:cover;border-radius:var(--radius-sm);border:1px solid var(--border);" onerror="this.parentElement.style.display='none'" />
                        </div>
                    </div>
                </div>
            </div>

            @* Durum *@
            <div class="form-section">
                <div class="section-header">
                    <div class="section-icon" style="background:#0a2d0a;color:#10b981;"><i class="fa-solid fa-toggle-on"></i></div>
                    <div><p class="section-title">Durum Ayarları</p><p class="section-subtitle">Kiralama ve sistem durumu</p></div>
                </div>
                <div class="section-body" style="padding:16px 24px;">
                    <div class="toggle-row">
                        <div class="toggle-label"><strong><i class="fa-solid fa-circle-check me-2" style="color:#10b981;"></i>Müsait</strong><span>Kiralamaya açık olsun</span></div>
                        <div class="form-check form-switch mb-0">
                            <input class="form-check-input" type="checkbox" id="IsAvailable" name="IsAvailable" value="true" checked />
                            <input type="hidden" name="IsAvailable" value="false" />
                        </div>
                    </div>
                    <div class="toggle-row">
                        <div class="toggle-label"><strong><i class="fa-solid fa-power-off me-2" style="color:var(--primary);"></i>Aktif</strong><span>Sistemde görünsün</span></div>
                        <div class="form-check form-switch mb-0">
                            <input class="form-check-input" type="checkbox" id="IsActive" name="IsActive" value="true" checked />
                            <input type="hidden" name="IsActive" value="false" />
                        </div>
                    </div>
                </div>
            </div>

            @* Özet *@
            <div class="form-section">
                <div class="section-header">
                    <div class="section-icon" style="background:#2d0a0a;color:var(--primary);"><i class="fa-solid fa-clipboard-list"></i></div>
                    <div><p class="section-title">Kayıt Özeti</p><p class="section-subtitle">Form dolduğunda güncellenir</p></div>
                </div>
                <div class="section-body" style="padding:14px 24px;">
                    <div id="sumEmpty" style="text-align:center;padding:10px 0;color:var(--text-muted);font-size:13px;">
                        <i class="fa-regular fa-file-lines" style="font-size:26px;display:block;margin-bottom:8px;opacity:.35;"></i>
                        Formu doldurmaya başlayın…
                    </div>
                    <div class="sum-row" id="sumPlate"><div style="display:flex;justify-content:space-between;"><span style="color:var(--text-muted);">Plaka</span><strong id="sumPlateVal" style="font-family:'Plus Jakarta Sans',sans-serif;letter-spacing:1px;"></strong></div></div>
                    <div class="sum-row" id="sumBrand"><div style="display:flex;justify-content:space-between;"><span style="color:var(--text-muted);">Marka / Model</span><strong id="sumBrandVal"></strong></div></div>
                    <div class="sum-row" id="sumYear"><div style="display:flex;justify-content:space-between;"><span style="color:var(--text-muted);">Yıl</span><strong id="sumYearVal"></strong></div></div>
                    <div class="sum-row" id="sumFuel"><div style="display:flex;justify-content:space-between;"><span style="color:var(--text-muted);">Yakıt / Vites</span><strong id="sumFuelVal"></strong></div></div>
                    <div class="sum-row" id="sumPrice"><div style="display:flex;justify-content:space-between;"><span style="color:var(--text-muted);">Günlük Fiyat</span><strong id="sumPriceVal" style="color:var(--primary);"></strong></div></div>
                </div>
            </div>

        </div>
    </div>

    <div class="action-bar">
        <div style="font-size:13px;color:var(--text-muted);">
            <i class="fa-solid fa-circle-exclamation me-1" style="color:var(--accent);"></i>
            <span style="color:var(--danger);">*</span> zorunlu alanlar
        </div>
        <div class="d-flex gap-2">
            <a href="/Admin/Car/CarList" class="btn-cancel-form"><i class="fa-solid fa-xmark"></i> Vazgeç</a>
            <button type="submit" class="btn-submit-car" id="submitBtn"><i class="fa-solid fa-check"></i> Aracı Kaydet</button>
        </div>
    </div>

</form>

@* QUICK-ADD MODALLER *@

<div class="quick-modal-backdrop" id="modal-brand">
    <div class="quick-modal">
        <div class="quick-modal-title"><i class="fa-solid fa-trademark"></i> Yeni Marka Ekle</div>
        <label class="form-label" for="qBrandName">Marka Adı <span class="required">*</span></label>
        <input type="text" id="qBrandName" class="form-control" placeholder="Örn: Skoda" maxlength="100" />
        <div class="quick-modal-error" id="qBrandError"></div>
        <div class="quick-modal-footer">
            <button type="button" class="btn-modal-cancel" onclick="closeModal('brand')">İptal</button>
            <button type="button" class="btn-modal-save" id="qBrandSaveBtn" onclick="quickSave('brand')"><i class="fa-solid fa-check"></i> Kaydet</button>
        </div>
    </div>
</div>

<div class="quick-modal-backdrop" id="modal-model">
    <div class="quick-modal">
        <div class="quick-modal-title"><i class="fa-solid fa-cube"></i> Yeni Model Ekle</div>
        <label class="form-label">Marka</label>
        <div id="qModelBrandInfo" style="font-size:13px;color:var(--text-muted);margin-bottom:14px;padding:8px 12px;background:#2a2a2a;border-radius:var(--radius-sm);"></div>
        <label class="form-label" for="qModelName">Model Adı <span class="required">*</span></label>
        <input type="text" id="qModelName" class="form-control" placeholder="Örn: Octavia" maxlength="100" />
        <div class="quick-modal-error" id="qModelError"></div>
        <div class="quick-modal-footer">
            <button type="button" class="btn-modal-cancel" onclick="closeModal('model')">İptal</button>
            <button type="button" class="btn-modal-save" id="qModelSaveBtn" onclick="quickSave('model')"><i class="fa-solid fa-check"></i> Kaydet</button>
        </div>
    </div>
</div>

<div class="quick-modal-backdrop" id="modal-category">
    <div class="quick-modal">
        <div class="quick-modal-title"><i class="fa-solid fa-tags"></i> Yeni Kategori Ekle</div>
        <label class="form-label" for="qCategoryName">Kategori Adı <span class="required">*</span></label>
        <input type="text" id="qCategoryName" class="form-control" placeholder="Örn: Cabrio" maxlength="100" />
        <div class="quick-modal-error" id="qCategoryError"></div>
        <div class="quick-modal-footer">
            <button type="button" class="btn-modal-cancel" onclick="closeModal('category')">İptal</button>
            <button type="button" class="btn-modal-save" id="qCategorySaveBtn" onclick="quickSave('category')"><i class="fa-solid fa-check"></i> Kaydet</button>
        </div>
    </div>
</div>

<div class="quick-modal-backdrop" id="modal-branch">
    <div class="quick-modal">
        <div class="quick-modal-title"><i class="fa-solid fa-building"></i> Yeni Şube Ekle</div>
        <div class="row g-3">
            <div class="col-12">
                <label class="form-label" for="qBranchName">Şube Adı <span class="required">*</span></label>
                <input type="text" id="qBranchName" class="form-control" placeholder="Örn: Kadıköy Şubesi" maxlength="100" />
            </div>
            <div class="col-12 col-sm-6">
                <label class="form-label" for="qBranchCity">Şehir <span class="required">*</span></label>
                <input type="text" id="qBranchCity" class="form-control" placeholder="Örn: İstanbul" maxlength="100" />
            </div>
            <div class="col-12 col-sm-6">
                <label class="form-label" for="qBranchAddress">Adres</label>
                <input type="text" id="qBranchAddress" class="form-control" placeholder="Örn: Bağdat Cad. No:1" maxlength="250" />
            </div>
        </div>
        <div class="quick-modal-error" id="qBranchError"></div>
        <div class="quick-modal-footer">
            <button type="button" class="btn-modal-cancel" onclick="closeModal('branch')">İptal</button>
            <button type="button" class="btn-modal-save" id="qBranchSaveBtn" onclick="quickSave('branch')"><i class="fa-solid fa-check"></i> Kaydet</button>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const _token = document.querySelector('input[name="__RequestVerificationToken"]')?.value ?? '';

        // ═══ MODAL ═══
        function openModal(type) {
            if (type === 'model') {
                const brandId   = document.getElementById('BrandId').value;
                const brandText = document.getElementById('BrandId').options[document.getElementById('BrandId').selectedIndex]?.text;
                if (!brandId) { alert('Önce bir marka seçin, ardından model ekleyebilirsiniz.'); return; }
                document.getElementById('qModelBrandInfo').textContent = '🏷 ' + brandText;
            }
            clearModalInputs(type);
            document.getElementById(`modal-${type}`).classList.add('open');
            setTimeout(() => { document.querySelector(`#modal-${type} input[type="text"]`)?.focus(); }, 100);
        }

        function closeModal(type) {
            document.getElementById(`modal-${type}`).classList.remove('open');
            clearModalInputs(type);
        }

        function clearModalInputs(type) {
            const modal = document.getElementById(`modal-${type}`);
            modal.querySelectorAll('input[type="text"]').forEach(i => i.value = '');
            const errEl = document.getElementById(`q${cap(type)}Error`);
            if (errEl) { errEl.textContent = ''; errEl.style.display = 'none'; }
            const btn = document.getElementById(`q${cap(type)}SaveBtn`);
            if (btn) { btn.disabled = false; btn.innerHTML = '<i class="fa-solid fa-check"></i> Kaydet'; }
        }

        function cap(s) { return s.charAt(0).toUpperCase() + s.slice(1); }

        document.addEventListener('keydown', e => {
            if (e.key === 'Escape') document.querySelectorAll('.quick-modal-backdrop.open').forEach(m => m.classList.remove('open'));
        });
        document.querySelectorAll('.quick-modal-backdrop').forEach(b => {
            b.addEventListener('click', function(e) { if (e.target === this) this.classList.remove('open'); });
        });
        document.querySelectorAll('.quick-modal input').forEach(input => {
            input.addEventListener('keydown', e => { if (e.key === 'Enter') quickSave(input.closest('.quick-modal-backdrop').id.replace('modal-', '')); });
        });

        // ═══ QUICK SAVE ═══
        async function quickSave(type) {
            const btn   = document.getElementById(`q${cap(type)}SaveBtn`);
            const errEl = document.getElementById(`q${cap(type)}Error`);
            errEl.style.display = 'none';
            btn.disabled = true;
            btn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Kaydediliyor…';

            try {
                let url = '', body = `__RequestVerificationToken=${encodeURIComponent(_token)}`;

                if (type === 'brand') {
                    const name = document.getElementById('qBrandName').value.trim();
                    if (!name) { showModalError(type, 'Marka adı boş olamaz.'); return; }
                    url = '/Admin/Car/QuickAddBrand';
                    body += `&brandName=${encodeURIComponent(name)}`;
                } else if (type === 'model') {
                    const name    = document.getElementById('qModelName').value.trim();
                    const brandId = document.getElementById('BrandId').value;
                    if (!name) { showModalError(type, 'Model adı boş olamaz.'); return; }
                    url = '/Admin/Car/QuickAddModel';
                    body += `&modelName=${encodeURIComponent(name)}&brandId=${brandId}`;
                } else if (type === 'category') {
                    const name = document.getElementById('qCategoryName').value.trim();
                    if (!name) { showModalError(type, 'Kategori adı boş olamaz.'); return; }
                    url = '/Admin/Car/QuickAddCategory';
                    body += `&categoryName=${encodeURIComponent(name)}`;
                } else if (type === 'branch') {
                    const name    = document.getElementById('qBranchName').value.trim();
                    const city    = document.getElementById('qBranchCity').value.trim();
                    const address = document.getElementById('qBranchAddress').value.trim();
                    if (!name) { showModalError(type, 'Şube adı boş olamaz.'); return; }
                    if (!city) { showModalError(type, 'Şehir boş olamaz.'); return; }
                    url = '/Admin/Car/QuickAddBranch';
                    body += `&branchName=${encodeURIComponent(name)}&city=${encodeURIComponent(city)}&address=${encodeURIComponent(address)}`;
                }

                const res  = await fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' }, body });
                const data = await res.json();

                if (!data.success) { showModalError(type, data.message ?? 'Bir hata oluştu.'); return; }

                addOptionToSelect(type, data.id, data.name);
                closeModal(type);
                if (type === 'brand') await loadModels(data.id);

            } catch (e) {
                showModalError(type, 'Sunucu hatası oluştu.');
            } finally {
                btn.disabled = false;
                btn.innerHTML = '<i class="fa-solid fa-check"></i> Kaydet';
            }
        }

        function showModalError(type, msg) {
            const errEl = document.getElementById(`q${cap(type)}Error`);
            errEl.textContent = msg; errEl.style.display = 'block';
            const btn = document.getElementById(`q${cap(type)}SaveBtn`);
            btn.disabled = false; btn.innerHTML = '<i class="fa-solid fa-check"></i> Kaydet';
        }

        function addOptionToSelect(type, id, name) {
            const map = { brand:'BrandId', model:'CarModelId', category:'CategoryId', branch:'BranchId' };
            const sel = document.getElementById(map[type]);
            if (!sel) return;
            const opt = document.createElement('option');
            opt.value = id; opt.textContent = name;
            sel.appendChild(opt);
            sel.value = id;
            sel.dispatchEvent(new Event('change'));
        }

        // ═══ MODEL AJAX ═══
        async function loadModels(brandId) {
            const sel = document.getElementById('CarModelId');
            sel.innerHTML = '<option value="">Yükleniyor…</option>';
            if (!brandId) { sel.innerHTML = '<option value="">— Önce marka seçin —</option>'; return; }
            try {
                const res  = await fetch(`/Admin/Car/GetModelsByBrand?brandId=${brandId}`);
                const json = await res.json();
                sel.innerHTML = '<option value="">— Model Seçin —</option>';
                if (json.success && json.data?.length > 0) {
                    json.data.forEach(m => {
                        const opt = document.createElement('option');
                        opt.value       = m.carModelId ?? m.modelId ?? m.id;
                        opt.textContent = m.modelName  ?? m.name;
                        sel.appendChild(opt);
                    });
                } else {
                    sel.innerHTML = '<option value="">Bu markaya model yok — + ile ekleyin</option>';
                }
            } catch (e) { sel.innerHTML = '<option value="">Model yüklenemedi</option>'; }
            updateSummary();
        }

        // ═══ YARDIMCILAR ═══
        function updatePlatePreview() {
            const val = document.getElementById('PlateNumber').value.trim();
            document.getElementById('platePreviewWrap').style.display = val.length >= 2 ? 'block' : 'none';
            document.getElementById('platePreviewText').textContent = val || '— — —';
            updateSummary();
        }

        function validatePlateInput(el) {
            const val = el.value.trim();
            const errEl = document.getElementById('plateError');
            if (!val) {
                el.classList.add('is-invalid');
                el.classList.remove('is-valid');
                errEl.innerHTML = '<i class="fa-solid fa-circle-exclamation"></i> Plaka zorunludur.';
                errEl.style.display = 'flex';
            } else if (!validatePlateNumber(val)) {
                el.classList.add('is-invalid');
                el.classList.remove('is-valid');
                errEl.innerHTML = '<i class="fa-solid fa-circle-exclamation"></i> Geçerli bir plaka giriniz. (Örn: 34 ABC 123)';
                errEl.style.display = 'flex';
            } else {
                el.classList.remove('is-invalid');
                el.classList.add('is-valid');
                errEl.style.display = 'none';
            }
            updateSummary();
        }

        function updateVinCount() { document.getElementById('vinCount').textContent = document.getElementById('VIN').value.length; }

        function adjustCounter(id, delta) {
            const el = document.getElementById(id);
            el.value = Math.max(parseInt(el.min)||0, Math.min(parseInt(el.max)||99, parseInt(el.value)+delta));
        }

        function updatePriceBanner() {
            const daily = parseFloat(document.getElementById('DailyPrice').value) || 0;
            const dep   = parseFloat(document.getElementById('DepositAmount').value) || 0;
            const b     = document.getElementById('priceBanner');
            if (daily > 0) {
                b.style.display = 'block';
                document.getElementById('bannerDaily').textContent   = '₺' + daily.toLocaleString('tr-TR');
                document.getElementById('bannerWeekly').textContent  = '₺' + (daily*7).toLocaleString('tr-TR');
                document.getElementById('bannerMonthly').textContent = '₺' + (daily*30).toLocaleString('tr-TR');
                document.getElementById('bannerDeposit').textContent = dep > 0 ? '₺'+dep.toLocaleString('tr-TR') : '—';
            } else { b.style.display = 'none'; }
        }

        function updateSummary() {
            const plate = document.getElementById('PlateNumber').value;
            const bSel  = document.getElementById('BrandId');
            const mSel  = document.getElementById('CarModelId');
            const year  = document.getElementById('Year').value;
            const fuel  = document.querySelector('input[name="FuelType"]:checked')?.value || '';
            const trans = document.getElementById('Transmission').value;
            const price = document.getElementById('DailyPrice').value;
            let hasAny  = false;
            function setRow(rId, vId, show, txt) {
                document.getElementById(rId).style.display = show ? 'block' : 'none';
                if (show) { document.getElementById(vId).textContent = txt; hasAny = true; }
            }
            setRow('sumPlate','sumPlateVal', !!plate, plate);
            setRow('sumBrand','sumBrandVal', !!(bSel.value && mSel.value), `${bSel.options[bSel.selectedIndex]?.text} ${mSel.options[mSel.selectedIndex]?.text}`);
            setRow('sumYear', 'sumYearVal',  !!year, year);
            setRow('sumFuel', 'sumFuelVal',  !!(fuel||trans), [fuel,trans].filter(Boolean).join(' / '));
            setRow('sumPrice','sumPriceVal', !!price, `₺${parseFloat(price||0).toLocaleString('tr-TR')} / gün`);
            document.getElementById('sumEmpty').style.display = hasAny ? 'none' : 'block';
        }

        function liveValidate(el) {
            el.classList.toggle('is-invalid', !el.value || el.value === '');
            el.classList.toggle('is-valid',   !!(el.value && el.value !== ''));
            updateSummary();
        }

        function switchImgTab(tab) {
            const u = tab === 'upload';
            document.getElementById('uploadPanel').style.display = u ? 'block' : 'none';
            document.getElementById('urlPanel').style.display    = u ? 'none'  : 'block';
            document.getElementById('tabUpload').classList.toggle('active', u);
            document.getElementById('tabUrl').classList.toggle('active', !u);
        }

        function handleFileSelect(e) { showFilePreviews(Array.from(e.target.files)); }
        function handleDragOver(e)   { e.preventDefault(); e.currentTarget.classList.add('dragover'); }
        function handleDragLeave(e)  { e.currentTarget.classList.remove('dragover'); }
        function handleDrop(e) {
            e.preventDefault(); e.currentTarget.classList.remove('dragover');
            showFilePreviews(Array.from(e.dataTransfer.files).filter(f => f.type.startsWith('image/')));
        }
        function showFilePreviews(files) {
            const grid = document.getElementById('imgPreviewGrid');
            const wrap = document.getElementById('imgPreviewWrap');
            grid.innerHTML = '';
            files.forEach(file => {
                if (file.size > 5*1024*1024) return;
                const reader = new FileReader();
                reader.onload = ev => {
                    const div = document.createElement('div');
                    div.style.cssText = 'border-radius:8px;overflow:hidden;';
                    div.innerHTML = `<img src="${ev.target.result}" style="width:100%;height:90px;object-fit:cover;"/><div style="font-size:11px;color:var(--text-muted);padding:3px 2px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">${file.name}</div>`;
                    grid.appendChild(div);
                };
                reader.readAsDataURL(file);
            });
            wrap.style.display = files.length > 0 ? 'block' : 'none';
        }
        function clearImages() {
            document.getElementById('imageFiles').value = '';
            document.getElementById('imgPreviewGrid').innerHTML = '';
            document.getElementById('imgPreviewWrap').style.display = 'none';
        }
        function previewUrlImg(url) {
            if (!url) return;
            document.getElementById('urlPreviewWrap').style.display = 'block';
            document.getElementById('urlImgPreview').src = url;
        }

        document.querySelectorAll('input[name="FuelType"]').forEach(r => {
            r.addEventListener('change', () => { document.getElementById('fuelError').classList.remove('show'); updateSummary(); });
        });

        // ═══ FORM SUBMIT ═══
        document.getElementById('carForm').addEventListener('submit', function(e) {
            const required = ['PlateNumber','VIN','BrandId','CarModelId','CategoryId','BranchId','Year','Kilometer','DailyPrice','DepositAmount','Transmission'];
            let valid = true;

            required.forEach(id => {
                const el = document.getElementById(id);
                if (!el || !el.value || el.value === '') { el?.classList.add('is-invalid'); valid = false; }
                else { el.classList.remove('is-invalid'); el.classList.add('is-valid'); }
            });

            // Plaka format kontrolü — validatePlateNumber script.js'de tanımlı
            const plateEl  = document.getElementById('PlateNumber');
            const plateErr = document.getElementById('plateError');
            if (plateEl.value && !validatePlateNumber(plateEl.value)) {
                plateEl.classList.add('is-invalid');
                plateEl.classList.remove('is-valid');
                plateErr.innerHTML = '<i class="fa-solid fa-circle-exclamation"></i> Geçerli bir plaka giriniz. (Örn: 34 ABC 123)';
                plateErr.style.display = 'flex';
                valid = false;
            }

            const fuel    = document.querySelector('input[name="FuelType"]:checked');
            const fuelErr = document.getElementById('fuelError');
            if (!fuel) { fuelErr.classList.add('show'); valid = false; }
            else        { fuelErr.classList.remove('show'); }

            // Validation başarısız → dur, butona dokunma
            if (!valid) {
                e.preventDefault();
                document.querySelector('.is-invalid')?.scrollIntoView({ behavior: 'smooth', block: 'center' });
                return;
            }

            // Validation geçti → spinner göster
            const btn     = document.getElementById('submitBtn');
            btn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Kaydediliyor…';
            btn.disabled  = true;
        });
    </script>
}
