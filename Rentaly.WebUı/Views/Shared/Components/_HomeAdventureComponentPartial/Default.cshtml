@model List<Process>

<section class="text-light jarallax" aria-label="section">
    <img src="/RentalyHTML/images/background/3.jpg" alt="" class="jarallax-img">
    <div class="container">
        <div class="row">
            <div class="col-lg-3">
                <h1>Maceranız Başlasın</h1>
                <div class="spacer-20"></div>
            </div>
            
            @foreach (var item in Model)
            {
                <div class="col-md-3">
                    <i class="fa @item.Icon de-icon mb20"></i>
                    <h4>@item.Title</h4>
                    <p>@item.Description</p>
                </div>
            }
        </div>
    </div>
</section>